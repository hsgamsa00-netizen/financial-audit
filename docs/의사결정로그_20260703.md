# 자율개발 의사결정 로그 (2026-07-02 밤 – 07-03 새벽)

사용자 수면 중 자율 위임에 따라 추천안으로 자체 진행한 결정 목록. 각 건은 사후 승인·번복 가능.

| # | 결정 | 사유 |
|---|---|---|
| D1 | 재무카드 recall 키워드 5종 보강(지체상금·과다지급·과오지급·과다 지급·계약금액 조정) | 표본검증(110건)에서 누락 2건 발견 → 보강 후 포착 확인. 정밀도 100% 유지 |
| D2 | sha256 중복 10그룹은 기록만, 원본 유지 | 감사원의 동일 문서 복수 srno 공개 관행 — 삭제 시 매핑 손상 |
| D3 | 감사 길잡이 딥링크 = 홈 연결 + 제목 복사 버튼 | Giljabi가 URL 검색 파라미터 미지원(실측). Giljabi 측 `?q=` 지원 추가는 별도 승인 백로그 |
| D4 | HWP→PDF 변환 체인 = pyhwp(hwp5html)→헤드리스 크롬 | 이 PC는 한컴 뷰어만 설치(COM 자동화 불가 실측). 표본 검수: 표·한글·서식 보존 양호. 변환본은 `pdf_conv` 별도 상태(원본 PDF와 구분·페이지 상이 가능성 정직 표기) |
| D5 | 변환 타임아웃 실패분 재시도 = 900초·병렬 2 | 실패 43건 전원 타임아웃(대용량). 900초로도 실패하는 초대형은 `hwp` 상태 유지(원문 보존됨·열람 PDF만 부재) |
| D6 | 검산기 1차 범위 = 등식형 30종(기관 분기), 지표형 67종은 위험 스크리닝(지자체 6종 인터랙티브+전체 참조 카드), 배점 22종은 참조 | 등식형만 완전한 입력필드 분해 가능. 지표 임계값은 지자체 재정위험 6종만 원문에 존재 |
| D7 | 사례 데이터 = 단일 슬림 JSON 2.3MB, 지연 로딩+로딩 표시 | Giljabi 샤드(10MB×8) 대비 소형 — 샤딩 오버엔지니어링 회피 |
| D8 | 울트라리뷰 = 클라우드판 직접 기동 불가 → 규정된 로컬 최대강도 폴백(10앵글→1표 검증→스윕)으로 수행 | 클라우드판은 사용자 터미널 `claude ultrareview` 필요. 기상 후 실행 권장 |
| D9 | 파이프라인 산출물(finance_cards.json 등 약 4.8MB)은 공개 레포에 유지 | 내용은 이미 공개된 카드 데이터·크로스PC 재현성 이득. 로컬 절대경로 파일만 gitignore |
| D10 | 연가보상비 워크시트 1차 제외 | 산식 원문(결산기준 3종 내) 미확보 — "기준 원문 인용만" 원칙에 따라 근거 없는 계산기 배제 |
| D11 | 감가상각 워크시트 = 정액법만 1차 | 정률법 상각률 표 원문 미확보. 동일 원칙 |
| D12 | Pages 배포 반복 실패 대응 = .nojekyll 추가 → 좀비 런 취소 시도 → 재실행 → 감시 지속 | 빌드는 성공·deploy 단계만 600초 타임아웃(GitHub 측). 재실행 #3이 취소 불가 좀비로 잔존(409) — 환경 큐 저해 의심. 로컬·저장소는 무영향 |
| D13 | 조서 초안·자료요구 목록 = md 텍스트 생성(복사/다운로드) | HWPX 생성은 감문도 연계(차기) — 1차는 이식 쉬운 플레인 텍스트 |

| D14 | 울트라리뷰 2회차 확정 중 2건 수용(wontfix): ①조립 스크립트의 인용 중복검증(빌드타임 수 초·무해) ②위험 위젯 RISK6 하드코딩(임계값이 자유서식 문자열이라 파싱이 더 취약 — 원문 수치 하드코딩+근거 병기 유지) | 고도(altitude) 지적은 타당하나 현 데이터 형태에서 데이터화가 오히려 오류원 |
| D15 | 검산기 저장 키를 안정 ID(R-해시)로 교체·기존 위치기반 키 저장값은 마이그레이션 없이 폐기 | 출시 전 단계·저장 사용자 없음 |
| D16 | Pages 배포 고착 해법 확정 = 적체 배포 객체 삭제(Deployments API) + 실패 deploy 잡 재실행 | 06:50 라이브 개통 확인. 좀비 워크플로 런은 관리자만 삭제 가능하나 무해 |
| D17 | 서비스워커 전략 = 셸 cache-first / 데이터 network-first(오프라인 폴백) | 데이터 신선도 우선 — 버전 범프 규율 없이도 stale 데이터 위험 없음 |

## 백로그(승인 대기·차기 제안)
- Giljabi에 `?q=` 검색 파라미터 지원 추가(양방향 딥링크 완성)
- 초대형 HWP 잔여 실패분: 업무 PC(정품 한글) 인쇄 변환 또는 제외 확정
- 조서 초안 HWPX 내보내기(감문도 파이프라인 연계)
- P3 LLM 질문팩 export
